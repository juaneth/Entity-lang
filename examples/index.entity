use: js

//When using JS package access to variables and other items are still available
var door = '🚪';

js(){
    const fs = require('fs');

    var file = fs.readFileSync('input.txt');

    console.log(file + door);
}